# Unconstrained array definitions are flagged.

@check(message="unconstrained array definition")
fun unconstrained_arrays(node) =
    node is ArrayTypeDef
    when node.f_indices is UnconstrainedArrayIndices
