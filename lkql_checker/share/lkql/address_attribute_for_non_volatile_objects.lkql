fun is_volatile(o) =
    o?.p_get_aspect_assoc("volatile") != null

@check
fun address_attribute_for_non_volatile_objects() =
	select a@AttributeRef
    when a.f_attribute?.p_canonical_text() == "address"
    and not is_volatile(a.f_prefix?.p_referenced_decl())
