# Flag all parameter specifications with more than N parameters with a default
# value.

@check(message="too many parameters with default value")
fun default_parameters(node, n: int = 0) =
    node is Params
    when (from (from node.f_params select ParamSpec(f_default_expr is Expr))
          select DefiningName).length > n

