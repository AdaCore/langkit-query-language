# Flag all parameters specifications with default value.
#
# All parameter specifications are checked: in subprograms (including formal,
# generic and protected subprograms) and in task and protected entries
# (including accept statements and entry bodies).

@check(message="default value for subprogram parameter")
fun default_parameters(node) = 
   node is ParamSpec when node.f_default_expr != null
