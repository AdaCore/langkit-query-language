# Relative delay statements are flagged. Delay until statements are not
# flagged.

@check(message="relative delay statement")
fun relative_delay_statements(node) =
   node is DelayStmt(f_has_until is UntilAbsent)