[1/2] use_for_of_loops: FOR loop may be replaced by a FOR OF loop
  at main.adb:6:4
--- a/main.adb
+++ b/main.adb
@@ -3,8 +3,8 @@
    C : Character;
 begin
    --  First violation
-   for I in S'Range loop
-      C := S (I);
+   for I of S loop
+      C := I;
    end loop;
 
    --  Second violation
Apply this fix? [y/n/a/A/q/h]
-> quit

0 fix(es) applied, 2 skipped, 0 conflict(s), 0 error(s).

Resulting files
===============

== main.adb ==
procedure Main is
   S : constant String := "Hello";
   C : Character;
begin
   --  First violation
   for I in S'Range loop
      C := S (I);
   end loop;

   --  Second violation
   for J in S'Range loop
      S (J) := C;
   end loop;
end Main;
