let nonConstant = query p when p is ConstantAbsent
let nonConstantObjects = query o when o is ObjectDecl &&
                                        o.has_constant in nonConstant
print (nonConstantObjects)
