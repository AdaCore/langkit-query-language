let subtype: List = query SubtypeIndication
let objects: List = query ObjectDecl
print([ o.image & " " & st.image | o <- objects, st <- subtype,
                                   (o.image & " " & st.image).length != 64])
