let subtype = select SubtypeIndication
let objects = select ObjectDecl
print([ o.image & " " & st.image | o <- objects, st <- subtype,
                                   (o.image & " " & st.image).length != 64])
