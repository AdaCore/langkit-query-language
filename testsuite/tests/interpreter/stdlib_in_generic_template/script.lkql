import stdlib

val gens = through follow_generics select BasicDecl
val igi = [
    { print(i); i }
    for i in gens if stdlib.in_generic_template(i)
].to_list # We use .to_list to evaluate the list, since we don't have for loops :)
