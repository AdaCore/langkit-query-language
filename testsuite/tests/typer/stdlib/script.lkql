# lkql version: 2

# The main purpose of this test is to import the stdlib in order to run the
# typechecking pass on it. Make sure there are no diagnostic in this file,
# otherwise the import will never be executed and thus the stdlib will never
# be typechecked.

import stdlib
