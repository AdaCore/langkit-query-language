description: Testing the behavior when passing a runtime without the 'Ada.Text_IO'
  package
driver: gnatcheck
project: prj.gpr
extra_args:
- --RTS=unknown
rules:
- recursive_subprograms
control:
- - SKIP
  - os != 'windows'
  - Disable this test on non-Windows systems due to target formatting
