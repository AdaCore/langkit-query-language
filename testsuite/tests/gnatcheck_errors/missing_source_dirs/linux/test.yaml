description: Testing the behavior when a source dir from the project file is missing
  (on Linux)
driver: gnatcheck
project: prj.gpr
format: brief
rules:
  - goto_statements
control:
  -   - SKIP
      - os == 'windows'
      - Disable this test on Windows due to path formatting
