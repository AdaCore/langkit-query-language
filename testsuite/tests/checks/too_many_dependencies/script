let maxDeps: int = 5

let result: List =
    query CompilationUnit(any c@children: Identifier(any parent(depth=2): WithClause))
            body
                l@LibraryItem
        when val semanticParent = l.item?.semantic_parent();
             val deps = [x | x <- c, x.referenced_decl() != semanticParent];
             deps.length > maxDeps

print(result)
