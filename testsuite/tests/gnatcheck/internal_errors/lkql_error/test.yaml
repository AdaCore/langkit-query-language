driver: gnatcheck
project: prj.gpr
rules_dirs:
  - rules
rules:
  - +Rfoo
  - +Rfoo_unit
control:
  - [XFAIL, "mode == 'ada' and os_name == 'nt'", "GNATcheck doesn't report LKQL exception on Windows"]