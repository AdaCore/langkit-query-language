driver: gnatcheck
description: Check the '-r' option behavior
format: brief
input_sources:
  - main.adb

tests:
  - label: Valid '-r' option
    rules:
      - goto_statements
      - Redundant_Null_Statements
  - label: Empty '-r' option
    extra_args:
      - -r
