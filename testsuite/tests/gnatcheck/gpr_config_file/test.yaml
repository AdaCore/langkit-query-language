description: Test the "--config" flag of GNATcheck by providing a custom naming scheme
  in the "config.cgpr" file.
driver: gnatcheck
project: prj.gpr
format: brief
rules:
- goto_statements
tests:
- label: Without config file
- label: With config file
  gpr_config_file: config.cgpr
