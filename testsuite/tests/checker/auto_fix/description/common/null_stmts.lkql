@check(
    auto_fix=(node, ctx) => [
        ctx.remove(n)
        for n in from node select NullStmt
    ].to_list,
    auto_fix_description="Remove the null statements"
)
fun null_stmts(node) = node is HandledStmts(any children: NullStmt)
